# 2026-09

<!-- Append. Never edit past entries. Newest at the bottom.
     Session heading convention adopted 2026-09-12; the 2026-09-01
     heading below was reformatted to match. Content untouched. -->

## Session — 2026-09-01

**Did:** Set up the project record. Committed to the Phase 0 workload (telematics frame
decoding at line rate) and recorded two opposed predictions with a discriminator, dated,
before any RTL exists.

**Broke / didn't work:** n/a — no RTL yet.

**Confused about:** Nothing yet, which is itself worth noting: this entry is the last one
that will be comfortable to write. From the next session on, this field is the point of
the file.

**Next:** Install iverilog, gtkwave, verilator. Run `make` in the starter kit — expect three
PASS lines. Open `build/sequential.vcd` and find where the two shift registers diverge.

---

## Session — 2026-09-12

**Did:**
- Installed the toolchain: Icarus Verilog 13.0, Verilator 5.050, GTKWave 3.3.107.
- Ran Project 1 end to end. Three PASS lines: mux2 8/8, ripple_adder 512/512, sequential
  counter checks all correct.
- Restructured the repo: `rtl/`, `tb/`, `scripts/`, `bench/`,
  `docs/{journal,decisions,results,experiments,bugs}`.
  The docs scaffold had never actually been unpacked — everything was flat in `files/`, with a
  fossilised sandbox export path (`files/mnt/user-data/outputs/...`) still committed.
- Froze decisions `0004-benchmark-first` and `0005-waveform-viewer`.
- Adopted `## Session — YYYY-MM-DD` headings in this file.

**Broke / didn't work:**
- **GTKWave installs but does not run on this Mac.** Launches and dies, printing nothing.
  This blocks the Phase 0 gate, which is specifically *"find a deliberately introduced bug by
  reading a waveform, without print statements"* — a gate that cannot be passed without a
  working viewer. *(Resolved same session: exit status is 137 = SIGKILL, not a crash. The
  Homebrew cask ships an x86_64-only binary dated October 2020; this is an arm64 Mac with no
  Rosetta, so the kernel kills it on exec. It has never run once. Replaced with Surfer 0.7.0,
  native arm64, which parses all three Project 1 VCDs — verified headlessly with `surfer server
  --file`. Written up as `decisions/0005`.)*
- **`make lint` was checking nothing and reporting success.** `-Wall` makes Verilator's
  cosmetic DECLFILENAME warning fatal, so both invocations aborted before analysing any logic,
  and a trailing `|| true` swallowed the non-zero exit. It had never once linted this code.
  Worse: the two hardcoded top modules (`counter`, `ripple_adder`) excluded `shift_blocking` —
  so the one module in the repo that is *wrong on purpose* was the one the linter could never
  see. Verilator flags it loudly (`BLKSEQ`) the moment you point it there. Fixed: `make lint`
  now covers 8 modules and fails on any warning; `make lint-trap` demonstrates the catch.
- README claimed the two shift registers diverge by three cycles. Measured from the testbench
  print: blocking asserts at cycle +0, non-blocking at cycle +2 — two cycles apart. The
  absolute latencies in the RTL comments (3 edges vs 1) were right; the relative claim was not.

**Confused about:**
- Why Icarus 13 rejected `logic` as the output of a gate primitive when this kit was verified
  under Icarus 12. *(Resolved same session: gate primitives drive their outputs structurally,
  and in this tool/version combination a primitive-driven signal needs net semantics — `wire`,
  not `logic`. `mux2_structural` now declares `output wire y` while the behavioral twin keeps
  `output logic y`. Writing down the resolution, but the confusion is the part worth keeping:
  the two modules look interchangeable in the source and are not interchangeable to the tool.)*
- The difference between structural and behavioral RTL — specifically, in what sense two
  descriptions that simulate identically are "the same hardware," and where that equivalence
  stops being true. Not resolved. Suspect the honest answer only arrives once something is
  actually synthesised, which is Phase 6.
- Whether a lint target that has *never* been green is worse than no lint target at all. Every
  run of it produced a confident "I am checking for latches" that was not true. Unresolved as a
  general habit question; the specific instance is fixed.

**Next:**
- **Attempt the Phase 0 gate**, now that it is reachable. `rtl/02_adder.sv`: study the
  ripple-carry structure, then README Exercise 2 — break `cout` deliberately, capture the
  failing waveform into `docs/bugs/0001-*/` **before** fixing it, and diagnose it from the
  waveform alone, with no `$display`.
- That capture is the first real use of the `bugs/` layer, and the evidence that the gate was
  actually passed rather than remembered as passed.

---

## Session — 2026-09-17

**Did:**
- Corrected a misreading of `0001` that had been quietly steering my reading list. FleetPoynt
  supplies the project's *motivation*; I had been treating it as a *curriculum* and reading
  telematics as an application domain — sensors, fleet workflows, dashboards. The core sees
  bytes. Almost none of that domain material can become a comparator, a state machine or a CRC.
- Froze `0006-reading-discipline`: read specifications and implementations, never domain
  material, with the operational test *"if it could plausibly contain a recommendation about
  FleetPoynt, it is product reading and belongs in a product session."*
- Named the benchmark: **Telematics Frame Decoder Benchmark**. FleetPoynt is context, never part
  of the artifact's name. Recorded in `bench/README.md`.

**Broke / didn't work:**
- Nothing built today. The failure was upstream of the keyboard: weeks of the wrong reading list,
  caught only because of what showed up in it (below).

**Confused about:**
- **The thing that actually caught this.** A general telematics explainer, requested in this
  project's context, came back containing a FleetPoynt architecture diagram with a telematics
  device placed inside it. I read it as coincidence at first. It isn't — domain material about a
  space you operate in drifts toward product recommendations, because that is what the genre is
  for. What unsettles me is that this is the roadmap's *own* documented failure mode ("letting
  the workload search decide a business question") arriving through a door I hadn't guarded:
  not from hunting for something to accelerate, but from ordinary background reading. I don't yet
  know what other doors are unguarded, and the rule I just wrote only closes this one.
- Whether the resync rate — the discriminator between predictions A and B — can be estimated at
  all from public sources, now that it has moved onto the critical path. `0001` deferred it on
  the assumption it wouldn't need answering yet. That assumption is spent; the question isn't.

**Found without writing any code:**
- GT06 frames start `7878` or `7979`. JT/T 808 frames start **and end** with `7e`. So the two are
  framed differently in kind — length-prefixed versus delimiter-terminated — and that is the
  *root* of the escaping asymmetry in Prediction B rather than a detail of it. One line of a
  protocol-identification table.
- Malformed and variant frames are routine in this family, not an edge case. Traccar has open
  JT808 decoder PRs and forum threads where a device speaks a variant the decoder chokes on
  (`readerIndex + length exceeds writerIndex` — a length-field disagreement causing a
  frame-boundary error, in production, in a mature codebase). The generator must emit malformed
  frames deliberately at a realistic rate; `bench/README.md` now says so.

**Next:**
- **`docs/bugs/0002-adder-unknown` is still open.** Mechanism withheld, 128/512 failing. Phase 0
  does not close until that is diagnosed from the waveform. Start with `stdout.txt`, not Surfer.
- Then read `Gt06ProtocolDecoder.java` — reference implementation before specification.

---

## Session — 2026-09-21

**Did:**
- **Closed the Phase 0 waveform gate.** Diagnosed `0002-adder-unknown` from `broken.vcd` with the
  mechanism withheld, and named it before decoding the spoiler: `cout` was driven from
  `carry[WIDTH-1]`, the carry *entering* the MSB, instead of `carry[WIDTH]`, the carry leaving it.
  The whole chain and every sum bit were correct; the fault was one wire at the module boundary.
  The pinning evidence is one line: at 15 ns `u_add.g_bit[3].u_fa.cout` reads 0 while the module's
  `cout` reads 1 — a module output contradicting the only signal that feeds its position.
- Verified the diagnosis instead of trusting it. A hand-simulated `cout = carry[3]` reproduces all
  128 observed failures exactly, none missed, no false positives.
- Fixed the spoiler decode command in `0002/NOTES.md`, which never ran on this machine: BSD
  `base64` requires `-i` for a file argument.
- **Built the ALU** (`rtl/04_alu.sv`): add, sub, and, or, xor, sll, srl, sra, slt, sltu. One
  `ripple_adder` from `02_adder.sv` serves all four arithmetic paths — sub is `a + ~b + 1`, and
  both comparisons are that subtraction with the difference discarded and a flag kept. `sltu` is
  the borrow (`~cout`); `slt` is `sign(diff) ^ overflow`.
- Encoded the ops as RV32I's `{funct7[5], funct3}` rather than inventing a numbering. ADD/SUB and
  SRL/SRA then differ in exactly one bit, as the ISA intends, which makes the Phase 2 decoder a
  wire instead of a lookup table. A free decision now that costs nothing and saves work twice.
- **Passed the Phase 1 ALU gate**: 207,232 cases against a golden model — 3,136 directed (every
  pair from a 14-value edge set × all 16 encodings), 4,096 exhaustive at `WIDTH=4`, 200,000
  randomised on a fixed seed so any failure reproduces. Signed/unsigned and shift masking are hit
  by construction, not by luck: the edge set contains 0, 1, −1, INT_MIN, INT_MAX and shift amounts
  31 / 32 / 33 / −32.
- Wrote `scripts/mutate-alu.sh` + `make mutate-alu`: injects seven classic ALU bugs one at a time
  and checks the bench reddens for each. All seven killed.
- **The mutation test paid for itself immediately, with a number I did not expect.** Six mutations
  died loudly — 6,000 to 36,000 failures each. One died by a thread: dropping SUB's carry-in
  (`a + ~b`, no `+1`) was caught by exactly **30** SLT/SLTU cases out of ~25,800. The reason is
  arithmetic, not luck: `a + ~b` is `a - b - 1`, so `~carry_out` yields `a <= b` where the answer
  wanted is `a < b`. **The two differ only when `a == b`.** And 30 is not approximately anything —
  it is 14 diagonal pairs from the directed edge set plus 16 from the 4-bit exhaustive sweep,
  exactly. The 200,000 randomised cases contributed **zero**, because `P(a == b) = 2⁻³²`.
  So: a random sweep two thousand times larger than the directed one found none of it. Directed
  edge sets are not a warm-up for randomisation and randomisation does not subsume them — equality,
  boundaries and identities have measure zero in a random space and must be placed by hand. Had I
  written the bench as "200k random cases, that's surely thorough", this bug would have shipped
  into the register file and then into the core.

**Broke / didn't work:**
- **`0003` — the first real bug of the project, and it was in the reference, not the design.**
  First `make alu` run: 6,080 failures, all of them SRA, none anywhere else. The model computed
  `(sa >>> sh) & mask`. `>>>` is arithmetic only while its left operand is signed, and
  SystemVerilog resolves signedness across the whole expression *before* evaluating it — `mask` is
  unsigned, so `sa` was converted to unsigned and `>>>` silently became `>>`. Nothing warns and
  the operator is still spelled `>>>`.
- I arbitrated before editing: −32 arithmetic-shifted right by 1 is −16 = `0xFFFFFFF0`, which is
  what the ALU produced; `0x7FFFFFF0` is the logical shift. **The ALU was right and my golden model
  was wrong.** Fixing the DUT to satisfy the broken reference would have corrupted correct hardware
  and then passed. That is the near-miss worth remembering, not the signedness rule.
- A comment in my own RTL stated the wrong reason for its own code. I wrote that `use_sub` cannot
  be `op[3]` because SRA has bit 3 set and must not invert `b`. Mutation testing refuted it: wiring
  `use_sub` to `op[3]` fails 9,728 cases and **every one is SLT or SLTU, not one is SRA** — because
  SRA's result never comes from the adder, so inverting `b` there is merely wasted power. The real
  constraint is the opposite direction: SLT and SLTU have bit 3 *clear* and both must subtract.
  Comment corrected to say what the mutation proved.

**Confused about:**
- Nothing blocking on the ALU. The open question is ordering (below), not design.

**Found without writing any code:**
- **The `sed`-delimiter tax.** Two mutation attempts failed to apply because SystemVerilog
  expressions are dense with `|`, `/` and `(`, which collide with every `sed` delimiter. A
  silently-unapplied mutation looks exactly like a *surviving* mutation — the bench appears blind
  when in fact nothing was ever changed. `scripts/mutate-alu.sh` therefore patches with Python and
  **exits non-zero if the target string is absent**, so "source moved" can never be misread as
  "bench is blind". A negative result needs its own integrity check.
- **A hypothesis list must span levels of hierarchy, not just vary within one.** Three candidates
  that are all corruptions of one Boolean expression inside one cell are not three hypotheses. Spread
  them across cell logic → interconnect → module boundary → integration *before* spreading them
  within any one level. `0002` was at the module boundary; a list confined to cell logic could not
  have contained the answer at any length.

**Next:**
- **The benchmark gate is the only thing still open in Phase 0, and it is now blocked on tooling.**
  `spike` and every `riscv*-gcc` are absent from this machine — checked, not assumed. Decision `0004`
  requires the reference result *before the core executes it*, and the roadmap puts the benchmark in
  Phase 0 **"in parallel"**, so building the ALU did not jump the queue. Installing the RISC-V
  toolchain + Spike is the next real task, and it is a Phase 0 item regardless of how far Phase 1 runs.
- Register file: two read ports, one write port, x0 hardwired to zero.

---

## Session — 2026-09-21 (second half): toolchain, and PRs

**Did:**
- **Installed the RISC-V toolchain.** `riscv64-elf-gcc` 16.2.0 came bottled from homebrew-core, so
  no source build. It carries `rv32i/ilp32`, `rv32im/ilp32`, `rv32imac/ilp32` multilibs, which is
  everything this project needs.
- **Built Spike from the tagged release rather than the Homebrew tap, deliberately.** The tap's
  formula is `url ".../riscv-isa-sim.git"` with `version "main"` — an unpinned branch, no source
  checksum — and its bottles target `sequoia` while this host is `tahoe`, so it would have compiled
  whatever sat on `main` today. Installing *the component that defines the golden result* from a
  moving target defeats having a golden result. Built v1.1.0 instead and verified the checkout
  against the tag listing: `530af85d83781a3dae31a4ace84a573ec255fefa`. It also sidesteps needing
  `brew trust` on a third-party tap.
- **Built `bench/rv32/`** — bare-metal RV32: `crt0.S`, an HTIF console, a linker script placing the
  image at Spike's `0x80000000`, and a Makefile exposing `ARCH` / `ABI` / `OPT` as overrides.
- **First real piece of the decoder ran on Spike.** `crc_itu_ref.c` computes CRC-ITU over a
  GT06-shaped frame. `crc_ref = 0x4cd4`, `acc = 0x33cc525d` — both matching an independent Python
  model written before the C was compiled, which is the only reason the numbers mean anything. 20
  consecutive runs are byte-identical, instruction count included.
- Moved to a **PR workflow**: pushed the 5 setup commits to `main`, then stacked PR #1 (Phase 0
  gate closure) → PR #2 (ALU) → PR #3 (this work).

**Broke / didn't work:**
- **The harness silently ate the last character of its own output.** `htif_exit` wrote the exit
  command without waiting for Spike to consume the pending `putchar`, so the trailing newline
  vanished — no error, no warning, just a missing byte. Caught only because `cat -e` was used to
  look at line endings rather than trusting that the output looked fine. A result whose final byte
  depends on a race is not a result. `htif_exit` now drains `tohost` first.
- **`scripts/capture.sh` recorded the wrong compiler.** It probed `riscv32-unknown-elf-gcc`, which
  does not exist on this machine; the actual compiler is `riscv64-elf-gcc`. Every manifest would
  have logged "not installed" for *the single most important provenance field in a Spike reference
  capture*, and it would have looked like a filled-in table. Fixed, and binutils added alongside.
  This was written weeks before there was any toolchain to check it against — an untested provenance
  field is not provenance.
- **zsh word-splitting cost me two broken probes.** `for spec in "rv32im ilp32"; set -- $spec` and
  `$CC $FLAGS ...` both pass the whole string as ONE argument, because zsh does not word-split
  unquoted expansions the way bash does. The first produced a bogus "gcc cannot target rv32"
  conclusion that was wrong; `--print-multi-lib` contradicted it within the same command, which is
  the only reason it was caught. The fix that matters is not "quote better" — it is that flag lists
  belong in a Makefile, where `sh` runs them and they are recorded, not in shell variables.

**Confused about:**
- Whether `instret` read from inside the program is the right work metric long-term, or whether it
  should come from Spike's own counters. It agrees with itself perfectly today; the question is what
  happens once there are interrupts and the core has a real pipeline.

**Found without writing any code:**
- **Homebrew's `riscv64-elf-gcc` is freestanding: there is no newlib, no `crt0.o`, no libc.**
  Compiling succeeds and *linking* is what fails, which is a confusing way to discover it. That
  turned out to be the right target rather than an obstacle — the core being built will have no libc
  either, so a reference measured through newlib's `printf` would partly be measuring newlib.
- An unpinned dependency is worst precisely where it looks most harmless: a simulator is "just a
  tool", right up until its version is the thing your denominator depends on.

**Next:**
- **`docs/experiments/0001-what-belongs-in-a-reference-result.md` is open and is mine to fill in.**
  Hypothesis first, before running anything. Sort the four output lines into reference versus
  configuration, then name the manifest parameters `bench/README.md` is missing.
- Register file: two read ports, one write port, `x0` hardwired to zero.

---

## Session — 2026-09-21 (third): checking a claim against the source

**Did:**
- Merged PRs #1, #2, #3. `main` is at the merge of the Spike reference work; all four testbenches
  pass and `bench/rv32` reproduces its result on merged main.
- **Took a circulating claim about protocol variance and checked it against the pinned Traccar
  source instead of accepting it.** The claim's own sources were weak — Alibaba product listings
  and Scribd uploads supporting technical assertions about protocol design. We have the reference
  implementations on disk at a pinned commit, so there was no reason to argue from summaries.
- Wrote `bench/PROTOCOL-EVIDENCE.md`: only what can be cited to a file and line at commit
  `847edd2c`, with an explicit section on what it does *not* establish.

**What the source actually says:**
- **GT06 is 16 protocols sharing an envelope.** `Gt06ProtocolDecoder.java:130` declares a 16-member
  `Variant` enum, tested at **35** branch sites, alongside **71** message-type constants tested at
  **110** sites, in 1,705 lines. `STANDARD` is one enum member, not a norm the others deviate from.
- **JT808 picks its framing, its escape alphabet and its header layout at runtime.** Three frame
  starts (`(`, `0x7e`, `0xe7`), found by a byte-at-a-time three-way scan; two different escape
  alphabets selected by the delimiter — the alternative mode has two introducer bytes and four
  pairs against the standard's one and two.
- **Header length is a function of three fields read from three different places:** `attribute`
  bit 14 (+1 byte, ID 6→10), the delimiter byte (ID 6→7 when `0xe7`), and the message *type*
  (index 2→1 for two types). You must read and branch on all three before you know where the body
  begins.

**Broke / didn't work:**
- **Two of the four recommendations I was handed were already satisfied, and recommending them
  meant not having read the documents.** The roadmap already reads "Phase 4 — Profile and choose
  the mechanism", not "the domain". And the negative-result allowance is not something to add: it
  is the roadmap's framing question at line 16, stated more sharply than the recommendation put it
  — *"a finding of 'the standard already covers it' is a real result… It is not a fallback,"* plus
  *"a genuinely possible Phase 5 finding is that custom space is never needed at all."*
  Advice about a repository is cheap to generate and expensive to apply; check it against the
  files before acting on it.
- The commonly-repeated version of the JT808 versioning story — "bit 14 selects 2013 vs 2019, phone
  number grows BCD[6]→BCD[10]" — is right but **incomplete**, and the missing parts are in the same
  six lines. It misses the `0xe7`→7-byte ID, which is not a version question at all, and misses
  that message type independently changes the index width.

**Confused about:**
- **The flag-lying claim is not in the same evidence class as the rest and I have marked it as
  such.** "Vendors ship 2013 payloads with the version bit set" comes from a library README, not
  from code this project has read. It is load-bearing for `0007` — if true, dispatch becomes
  speculative parse-validate-fallback rather than a lookup — so it needs a source in the
  `decisions/0006` class or explicit marking as a benchmark assumption. It is currently marked.
- Whether variant *frequency* can be estimated at all from public sources. 16 variants in a decoder
  says nothing about what fraction of traffic is non-`STANDARD`, and the benchmark needs that
  number as a declared parameter.

**Found without writing any code:**
- **Predictions A and B are not cleanly separable, and the source shows exactly where they fuse.**
  The JT808 destuffing loop is per-byte work (B) containing a variant branch selected by the
  delimiter (A). A benchmark built as though the two are independent would measure neither.
- The resync cost in Prediction A is not hypothetical. It is a literal byte-at-a-time three-way
  comparison loop in `Jt808FrameDecoder.java`, skipping one byte at a time until a frame start
  appears.

**Next:**
- **`docs/decisions/0007` is open and is mine to decide:** stateless per-frame detection, cached
  per-device detection, or both as a declared benchmark axis. It must be settled before the
  reference decoder is written, because it may move the profile more than any accelerator.
- `docs/experiments/0001` still open — hypothesis first.

---

## Session — 2026-09-21 (fourth): deciding 0007

**Did:**
- Merged PR #4. **Decided `0007`:** the stateless decoder is *the* reference result; the cached
  decoder is a declared second mode reported beside it; the difference between them is the
  dispatch-cost measurement.
- Updated `bench/README.md` with the manifest parameters that follow: `detection_mode` on every
  result, plus connection count and frames-per-connection for cached runs only.

**The argument that actually decided it** — and it was not the one I expected going in. I had this
filed as a performance-realism question: stateless is the honest worst case, cached is closer to
deployment, pick your trade-off. It is not that. Reading the recorded predictions side by side:

```
A  "frame synchronisation and PER-MODEL DISPATCH dominate"
B  "frame sync and dispatch are O(1) PER FRAME; the checksum is O(n)"
```

A cached decoder makes dispatch O(1) per *connection* rather than per frame. **That is
Prediction B's position, implemented.** Building the reference that way would bake one side of the
dispute into the artifact whose only job is to settle it. Stateless does not assume A is right —
it declines to optimise. One option pre-judges the question and the other does not, and that
asymmetry decides it on its own. The realism trade-off never had to be adjudicated at all.

**Broke / didn't work:**
- **I put a wrong number in my own options list, and it argued against the option that won.**
  Option 3 was written as "roughly doubles reference-decoder work." It does not: detection sits
  behind one interface and the decoder body is shared, so the second mode is a session map plus a
  revalidate-on-failure path — call it 15%. Corrected in place with a note rather than quietly
  dropped, because the whole point of keeping rejected options is to show what the choice was
  weighed against, and a cost estimate that was wrong by 6× is exactly the kind of thing that
  silently steers a decision. Worth noticing that the estimate was invented at the moment of
  writing and never checked against anything.

**Found without writing any code:**
- **Prediction A is two costs, not one, and the project only had an instrument for one of them.**
  A bundles *resync* (finding frame starts after garbage) with *dispatch* (choosing which variant
  decoder runs). Resync rate is already a declared stimulus parameter and the roadmap calls it
  "the discriminator." Dispatch had nothing. The stateless↔cached delta is that missing
  instrument, so running both modes decomposes A into two independently measurable halves rather
  than one lump — strictly more than Phase 4 could otherwise conclude.
- That reframes why the second mode gets built in Phase 0 instead of deferred: not for realism,
  but because it is a *measuring instrument*, and `0004`'s argument about artifacts built after
  profiling applies to instruments as much as to benchmarks.

**Next:**
- `docs/experiments/0001` is still open — hypothesis before running.
- The benchmark specification, now that `0007` no longer blocks its section 6.
- Register file: two read ports, one write port, `x0` hardwired to zero.

---

## Session — 2026-09-21 (fifth): three of my own justifications did not survive

**Did:**
- Merged PR #5. Then had `0007` reviewed, and **amended it rather than defending it.** The
  decision stands — both modes built, delta as instrument — but three claims I used to justify it
  were wrong.
- Recorded **Prediction C** in the roadmap, dated, before any data exists.

**What was wrong, in order of how much it mattered:**

- **"Stateless declines to optimise" was the wrong frame.** Stateless is not the absence of a
  choice — it sets per-frame dispatch to its *maximum*, exactly as cached sets it near its
  minimum. They bracket the term from opposite ends and neither is neutral. The core argument
  survives (a cached reference would implement B's position inside the artifact meant to settle A
  vs B; stateless does not presuppose A), but calling the stateless figure "the reference result"
  unqualified invites Phase 4 to read a *bound* as a *finding*, in the direction favouring A.
  Resolved by separating the two uses: stateless is the denominator for platform speedup claims,
  which is all `0004` ever meant; the stateless–cached **range** is what characterises the
  workload. Neither endpoint is quoted alone as a description.
- **"O(1) per connection" is false at line rate**, which is the rate `0001` commits to. It assumes
  frames from one device arrive together. They interleave, so every frame needs a state lookup.
  Caching does not remove a per-frame cost — it trades a branch-heavy one for a memory-heavy one.
- **The delta does not measure "dispatch cost."** It measures *detection minus lookup*, and which
  dominates is set by connection count: few connections and the delta approximates detection; many
  interleaved with a miss per frame and it shrinks to nothing or inverts. Connection count was
  written down as a parameter of the cached run. It is not — it decides what the second instrument
  measures at all, so it is promoted to a discriminator alongside resync rate.
- **The 15% estimate had the same provenance as the "roughly double" it replaced.** Invented while
  writing, never checked, against code that does not exist — and carrying the locality assumption
  the point above demolishes. I had written up replacing it as if it were a correction. It was
  not; it was a better-reasoned guess wearing a correction's authority. Marked in `0007` as an
  unchecked estimate to be measured. **Replacing a bad estimate with a better-reasoned one is not
  the same as checking it**, and I did not notice the difference while congratulating myself on
  the first catch.

**Broke / didn't work:**
- Nothing in the tree. The failures this session were all in reasoning I had already committed.

**Found without writing any code:**
- **Device identity is carried by the connection, not the frame — verified, and unconditional for
  GT06.** `Gt06ProtocolDecoder.java:526` resolves by IMEI *only* inside `if (type == MSG_LOGIN)`;
  line 502 handles every other type with `getDeviceSession(channel, remoteAddress)` and no id.
  Non-login GT06 frames carry no device identifier at all. **A flat byte stream therefore cannot
  represent the GT06 workload even in principle.** `bench/README` and the input contract updated.
- **A circularity argument I was given is wrong, and the correct reasoning narrows its scope.**
  The claim: JT808 lookup is circular because the terminal number sits at a different offset in
  2013 vs 2019, so you need the version to read it. False — `attribute` is at a fixed offset and
  bit 14 announces the version directly; line 366 locates the ID without knowing anything in
  advance. The conclusion survives by a different route: if the flag *lies*, the decoder slices 10
  bytes where the ID is 6 and the key is garbage — unusable precisely when the cache matters. The
  route decides the scope: on the stated reasoning, transport keying is required always; on the
  real one, always for GT06 (verified) and only-if-flag-lying for JT808 (unsourced).

**Next:**
- Benchmark specification. Its input-contract section now has three verified constraints to honour
  rather than being written from imagination.
- `docs/experiments/0001` — still open, still mine, hypothesis first.
- **Guard:** the roadmap's Phase 0 benchmark item is the benchmark *running on Spike*, not a
  specification of it. `crc_itu_ref.c` is a harness proof, not the benchmark. Do not let the
  register file start and close that gap by default.

---

## Session — 2026-09-21 (sixth): the specification

**Did:**
- Wrote `bench/SPEC.md` and froze it before the reference decoder exists, per `0004`. Eight
  sections: protocols and variants, frame types, input/output contract, malformed classes,
  resync model, detection strategy, size, provenance.
- **Marked every line as one of three kinds** — `[V]` verified to a file and line, `[D]` fixed by
  a decision record, `[ ]` unset. The mark is the point of the document. Twice this month a number
  invented while writing has steered a decision, so the spec makes the difference between
  "verified", "decided" and "nobody knows" impossible to skim past.
- Verified all seven frame-type constants and both resync targets against the pinned source before
  committing, rather than trusting the ones I had copied out earlier.

**Choices worth recording:**
- **Frame types are chosen by distinct *work shape*, not by coverage.** Seven types, each earning
  its place: `MSG_LOGIN` because it is the only GT06 frame carrying an IMEI; `MSG_STATUS` for the
  short-frame regime where per-frame overhead beats per-byte work; `MSG_LOCATION_BATCH` because N
  records under one header is a per-frame shape nothing else has; and `MSG_LOCATION_REPORT_2`
  (`0x5501`) because it is the **only** way to exercise the type→header-length branch — it reads a
  1-byte index where every other type reads 2.
- **GT06 variants are fixed at `STANDARD` for draft 1.** The other 15 multiply branch coverage
  without adding a new *kind* of work, and the variant mix is unset anyway — including them would
  have meant inventing proportions, which is the failure this spec is built to prevent.
- **Output emits a record for rejected frames too.** A decoder that silently drops malformed input
  is indistinguishable from one that mis-parses it.
- **No floating point in the output contract.** A reference result must not depend on FP rounding
  differing between Spike and the core.
- **Target rate is explicitly not a benchmark parameter.** The benchmark counts instructions for a
  fixed input; rate is a deployment property and belongs in Phase 4's interpretation.

**Broke / didn't work:**
- Nothing. But the spec's own count is uncomfortable and worth stating plainly: **ten parameters
  are unset and none of them has a source.** Protocol mix, variant mix, resync rate, garbage-run
  length, flag-lying rate, per-class malformed rates, frame count, connection count, interleaving
  pattern, frame length distribution. Two are swept rather than set (resync rate and connection
  count, both named discriminators). The other eight are assumptions waiting to be declared as
  such. That is the honest state, and collecting them in one block at the end of the spec is so
  that no one can fill one in by accident.

**Found without writing any code:**
- **The input contract was the section most likely to have been written wrong from imagination.**
  Without Finding 3 it would have said "deterministic byte streams" — flat, frame-aligned, no
  connection identity — and the GT06 half of the benchmark would have been unrepresentable rather
  than merely inaccurate. Reading `getDeviceSession(channel, remoteAddress)` at line 502 changed a
  structural decision, not a detail.
- Chunks in the stimulus are deliberately **not** frame-aligned. A stream does not deliver whole
  frames, and frame-aligned input would quietly remove the partial-state handling that is a real
  part of the work.

**Next:**
- The **stimulus generator**, from §1–§5 and §8. `bench/README` predicts it will be harder than the
  reference decoder, because the decoder has a specification and the generator has to invent a
  traffic model. That prediction is now testable.
- `docs/experiments/0001` — still open, still mine.
- **Guard, repeated:** Phase 0's benchmark item is the benchmark *running on Spike*. A frozen spec
  is not that. `crc_itu_ref.c` is a harness proof, not the benchmark.

---

## Session — 2026-09-25: experiment 0001, answered

**Did:**
- Ran the 0001 sweep, then step 4 — the judgement. Captured Method step 1 and a stale-build run at
  the hypothesis commit *before* touching the harness, fixed the harness, then captured all twelve
  builds the write-up cites. All twelve pass `make check`; all from a clean tree.
- Drafted 0001's Result, Outcome and What this changes, for the author to edit before merge.
- `bench/README` got the build parameters 0001 had promised. `decisions/0008` and
  `experiments/0002` follow in stacked PRs.

**Broke / didn't work:**
- **The harness reported the wrong configuration** (`docs/bugs/0004`). `make run OPT=-O3` over an
  -O2 build re-ran the -O2 binary, with a clean tree and a manifest that was true in every field.
- **My first fix failed the same way, more quietly.** A flags stamp only works if make can tell the
  stamp is newer, and this make compares mtimes to the second. Switch configurations inside one
  second — as any capture loop does — and the old binary runs. Build directories are now named by
  a hash of their inputs; no timestamps.
- **The previous session's explanation of -O2 was wrong.** It said GCC produced "a closed form built
  from shifts and masks". It was a 512-byte lookup table GCC generated, and the masks were only the
  bit-reflection wrapped around it. The table was missed. It was said in chat and never reached the
  repo. Twice this month an invented *number* steered a decision; this was an invented *mechanism*,
  caught one step before it would have been recorded.
- The GT06 login example in `crc_oracle.py` is not pinned to a document and page. There is no PDF
  reader on this host, so it is marked `[ ]` and the RevEng check value carries the weight.
  **→ Resolved the same session.** Installed poppler with auto-update and dependent upgrades
  switched off. 47 formulae were added and none upgraded, and the rebuilt reference image came out
  byte-identical, so the image hash did the check it exists for. The example is in §5.1.3 of the
  GT06 v1.8.1 document (PDF page 12, pinned by SHA-256 in `reference/README.md`) and is now
  `[V]`, along with the server's response from the same page. Appendix A's `crctab16` is the
  table `decisions/0008` adopts, entry for entry.

**Confused about:**
- Why GCC's table fallback for a *reflected* CRC builds the forward table and reflects every byte,
  rather than building the reflected table that the textbook algorithm uses with no reflection at
  all. On rv32im the reflection costs almost everything the table saves.
- Why -O2 without the CRC pass turns the per-bit branch into a mask. It is not the target's branch
  cost: `-mbranch-cost=1` through `4` all produce the same code. Some earlier, target-independent
  transformation — not identified.
- Whether a compiler that predates `-foptimize-crc` rejects `-fno-optimize-crc`. It should, as an
  unknown `-f` option, but this is untested — and 0004's Cortex-M4 baseline will depend on it.

**Next:**
- The author edits 0001's write-up, then merges.
- `experiments/0002`: the author's prediction goes in first. Then the sealed one is opened, and only
  then is anything built.
- The stimulus generator is still the Phase 0 item. **Guard, repeated:** `crc_itu_ref.c` is a
  harness proof, not the benchmark.
